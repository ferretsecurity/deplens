# OpenAI and GitHub credentials are intentionally not stored here.
#
# Sandcastle stages/copies $CODEX_HOME/auth.json (falling back to
# ~/.codex/auth.json) into a writable sandbox CODEX_HOME, and bind-mounts
# ~/.config/gh read-only for GitHub CLI authentication.
