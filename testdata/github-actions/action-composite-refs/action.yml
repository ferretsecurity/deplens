name: Composite refs
description: Uses tag and branch action references
runs:
  using: composite
  steps:
    - uses: actions/setup-node@v6
    - uses: actions/cache@main
    - uses: actions/aws/ec2@main
