project('dependency_versions', 'cpp', meson_version : '>=0.63.0')

fmt_dep = dependency('fmt', version : '>=10.0')
boost_dep = dependency('boost', version : '>=1.82', modules : ['filesystem', 'program_options'])
qt_dep = dependency('qt6', version : '>=6.6', modules : ['Core', 'Gui'])
