name = "config_legacy_dev"
version = "0.1.0"

[dependencies]
gleam_stdlib = ">= 0.65.0 and < 1.0.0"

# Retained for backwards compatibility; current projects use dev_dependencies.
[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
