version = "0.7.0"
author = "demo"

# A direct repository, an explicit revision, a versioned repository, and HEAD.
requires "https://github.com/example/direct-git#5a54b5e"
requires "https://bitbucket.org/example/direct-hg >= 2.0.0"
requires "unreleased-tool#head"
